import SwiftUI

struct MoodView: View {
    var body: some View {
        VStack {
            Text("Daily Mood (placeholder)")
                .font(.title2)
            Text("Ежедневный трекинг настроения")
                .foregroundColor(.secondary)
        }
        .padding()
    }
}
